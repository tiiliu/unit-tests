export const angleCalculator = (turns) => {
  if (turns === 0) {
  return 360 * turns
  } else if (turns === 1) {
    return 360 * turns
  } else if (turns === 1.5) {
    return 360 * turns
  } else if (turns === 2) {
    return 360 * turns
  }
}
